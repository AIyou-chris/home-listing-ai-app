services:
  - type: web
    name: home-listing-ai-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: HUSKY=0 npm install && npm run build && cd backend && npm install
    startCommand: cd backend && node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
