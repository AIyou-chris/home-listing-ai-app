services:
  - type: web
    name: home-listing-ai-backend
    runtime: node
    region: oregon
    plan: free
    buildCommand: npm install --ignore-scripts && npm run build && cd backend && npm install --ignore-scripts
    startCommand: cd backend && node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
